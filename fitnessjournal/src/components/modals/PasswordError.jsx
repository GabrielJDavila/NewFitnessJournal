
export default function PasswordError() {
    return (
        <div className="password-error">
            <p className="password-error-text">* Password must contain at least one uppercase letter "A-Z", one special character, and one number.</p>
        </div>
    )
}